---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    api-approved.kubernetes.io: unapproved
    controller-gen.kubebuilder.io/version: v0.18.0
  name: clusterregistrycredentials.registry.k8s.io
spec:
  group: registry.k8s.io
  names:
    kind: ClusterRegistryCredential
    listKind: ClusterRegistryCredentialList
    plural: clusterregistrycredentials
    shortNames:
    - crc
    singular: clusterregistrycredential
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterRegistryCredential is the Schema for the clusterregistrycredentials
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          sops:
            description: |-
              Sops contains the SOPS metadata block for decryption
              This field is managed by SOPS and should not be edited manually.
            type: object
            x-kubernetes-preserve-unknown-fields: true
          spec:
            description: ClusterRegistryCredentialSpec defines the desired state of
              ClusterRegistryCredential
            properties:
              decryption:
                description: Decryption contains the configuration for decrypting
                  the provider spec.
                properties:
                  provider:
                    description: Provider is the decryption provider to use, e.g.,
                      "sops".
                    type: string
                required:
                - provider
                type: object
              provider:
                description: Provider contains the configuration for the registry
                  provider.
                properties:
                  ghcr:
                    description: GHCR provides credentials for the GitHub Container
                      Registry.
                    properties:
                      apiEndpoint:
                        description: |-
                          APIEndpoint is the GitHub API endpoint.
                          Defaults to "https'://api.github.com" if not specified.
                        type: string
                      appId:
                        description: AppID is the unique identifier for the GitHub
                          App.
                        type: string
                      installationId:
                        description: InstallationID is the identifier for the installation
                          of the GitHub App.
                        type: string
                      permissions:
                        additionalProperties:
                          type: string
                        description: Permissions specifies the permissions to request
                          for the installation token.
                        type: object
                      privateKey:
                        description: |-
                          PrivateKey is the RSA private key of the GitHub App.
                          This field is populated by the controller after SOPS decryption.
                          It should be present in the 'sops' block of the CR, not set directly.
                        type: string
                      registry:
                        description: |-
                          Registry is the container registry endpoint.
                          Defaults to "ghcr.io" if not specified.
                        type: string
                    required:
                    - appId
                    - installationId
                    type: object
                type: object
              rotation:
                description: Rotation contains the policy for rotating the credentials.
                properties:
                  rotateBefore:
                    description: |-
                      RotateBefore is the duration before token expiration to trigger a rotation.
                      Defaults to "15m".
                    type: string
                type: object
              templates:
                items:
                  description: SecretTemplateSpec defines a template and the list
                    of targets it applies to.
                  properties:
                    data:
                      additionalProperties:
                        type: string
                      description: |-
                        Data contains the string templates for the Secret's data map.
                        The templates are rendered using Go's text/template engine.
                        Available variables: .Token, .Username, .DockerConfigJSON, .ExpiresAtRFC3339, .Registry
                      type: object
                    metadata:
                      description: Metadata contains optional labels and annotations
                        to merge into the target Secrets.
                      properties:
                        annotations:
                          additionalProperties:
                            type: string
                          type: object
                        labels:
                          additionalProperties:
                            type: string
                          type: object
                      type: object
                    name:
                      description: Name is a logical name for this template group
                        (e.g., "dockerconfig-logins")
                      type: string
                    targets:
                      description: Targets is the list of Secrets to create using
                        this template.
                      items:
                        description: SecretTarget defines the name and namespace of
                          a target Secret.
                        properties:
                          name:
                            description: Name of the Secret.
                            minLength: 1
                            type: string
                          namespace:
                            description: Namespace of the Secret.
                            minLength: 1
                            type: string
                        required:
                        - name
                        - namespace
                        type: object
                      minItems: 1
                      type: array
                    type:
                      default: kubernetes.io/dockerconfigjson
                      description: Type is the type of the Kubernetes Secret (e.g.,
                        "Opaque", "kubernetes.io/dockerconfigjson").
                      type: string
                  required:
                  - data
                  - targets
                  - type
                  type: object
                minItems: 1
                type: array
            required:
            - provider
            - templates
            type: object
          status:
            description: ClusterRegistryCredentialStatus defines the observed state
              of ClusterRegistryCredential
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the resource's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastRotationTime:
                description: LastRotationTime is the timestamp of the last successful
                  token rotation.
                format: date-time
                type: string
              tokenExpirationTime:
                description: TokenExpirationTime is the timestamp when the current
                  token expires.
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
